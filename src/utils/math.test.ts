import { describe, it, expect } from 'vitest';

// Minimal smoke test — verifies the test runner is wired up correctly.
describe('smoke', () => {
  it('true is true', () => {
    expect(true).toBe(true);
  });
});
