# Lifecycle script allowlist — block all by default, allow specific packages.
# pnpm v10 blocks lifecycle scripts from third-party packages by default.
# To permit a package to run install scripts, add it explicitly:
#   onlyBuiltDependencies[]=<package-name>
onlyBuiltDependencies[]= 
